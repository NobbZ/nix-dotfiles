pre-commit:
  parallel: true
  commands:
    formatting:
      glob: "*.nix"
      run: nixfmt -c {all_files}
    gitlab-yml:
      run: dhall-to-yaml < _gitlab/gitlab-ci.dhall | diff .gitlab-ci.yml -
